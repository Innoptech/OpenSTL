[build-system]
requires = ["setuptools>=42", "wheel", "cmake>=3.15.3"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-ra -q"
testpaths = [
    "tests/python",
]

[tool]
[tool.commitizen]
name = "cz_conventional_commits"
version = "1.1.1"
tag_format = "v$version"


[tool.poetry.dependencies]
pybind11 = "v2.11.1"
catch2 = "v3.5.3"
